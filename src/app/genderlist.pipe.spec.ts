import { GenderlistPipe } from './genderlist.pipe';

describe('GenderlistPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderlistPipe();
    expect(pipe).toBeTruthy();
  });
});
